# Directories
alias l='ls -alhFXG --group-directories-first -A -v --color=auto'
alias ll='l'
alias lll='l'
alias app='cd /app/'

# GIT
alias gs='git status'
alias gp='git push'
alias gc='git commit'

# Utilities
alias myip='curl http://ipecho.net/plain; echo'
